#set text( 
  lang: "de"
)

#outline()

#heading(level: 2, [
  dinge machen
])

#heading(level: 3, [
  wieso auch nicht?
])


Die *a-b-c-Formel* kann Quadratische Gleichungen lösen.

$
x_{1,2} = frac(-b sqrt(b^2 - 4a c), 2a)
$


Oder *p-q-Formel*: 

$
x_{1,2} = - frac(p,2) +- sqrt( frac(p,2)^2 -q )
$

Ich verstehe nicht so wirklich nach welchem Muster der *Preview* Server sterben geht und wann nicht.  
